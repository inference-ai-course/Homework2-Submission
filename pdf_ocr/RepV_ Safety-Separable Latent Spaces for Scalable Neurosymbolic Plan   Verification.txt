arXiv:2510.26935v1 [cs.RO] 30 Oct 2025

REPV: SAFETY-SEPARABLE LATENT SPACES FOR SCALABLE
NEUROSYMBOLIC PLAN VERIFICATION

Yunhao Yang! Neel P. Bhatt! Pranay Samineni! Rohan Siva! Zhangyang Wang' Ufuk Topcu!

ABSTRACT

As Al systems migrate to safety—critical domains, verifying that their actions comply with well-defined rules
remains a challenge. Formal methods provide provable guarantees but demand hand-crafted temporal-logic
specifications, offering limited expressiveness and accessibility. Deep learning approaches enable evaluation
of plans against natural-language constraints, yet their opaque decision process invites misclassifications with
potentially severe consequences. We introduce RepV, a neurosymbolic verifier that unifies both views by learning
a latent space where safe and unsafe plans are linearly separable. Starting from a modest seed set of plans
labeled by an off-the-shelf model checker, RepV trains a lightweight projector that embeds each plan, together
with a language model-generated rationale, into a low-dimensional space; a frozen linear boundary then verifies
compliance for unseen natural-language rules in a single forward pass.

Beyond binary classification, RepV provides a probabilistic guarantee on the likelihood of correct verification
based on its position in the latent space. This guarantee enables a guarantee-driven refinement of the plan-
ner, improving rule compliance without human annotations. Empirical evaluations show that RepV improves
compliance prediction accuracy by up to 15% compared to baseline methods while adding fewer than 0.2 M
parameters. Furthermore, our refinement framework outperforms ordinary fine-tuning baselines across various
planning domains. These results show that safety-separable latent spaces offer a scalable, plug-and-play primitive

for reliable neurosymbolic plan verification. Code and data are available at: https://repv-project.github.io/

1 INTRODUCTION

As Al systems take on greater roles in safety-critical ap-
plications, ensuring their actions comply with well-defined
rules remains challenging. Compliance is a foundation for
safety, reliability, and accountability across healthcare (Os-
ifowokan et al., 2025; Sbodio et al., 2024; Daram, 2025),
finance (Deshpande, 2024; Balakrishnan, 2024), and au-
tonomous systems (Yazdanpanah et al., 2023; He et al.,
2021). Traditional rule-based systems enable formal veri-
fication against specified logic-based constraints (Grosan
et al., 2011). In contrast, Al systems, driven by deep learn-
ing models, lack interpretability and formal guarantees due
to their black-box nature, which increases the difficulty of
rule compliance verification.

Formal methods and language models provide two ap-
proaches for verifying compliance, yet each has its limi-
tations. Existing works have extended formal methods tech-
niques to support compliance verification across multiple
input modalities, including natural language and program-

The University of Texas at Austin, United States. Correspon-
dence to: Yunhao Yang <yunhaoyang234@utexas.edu>, Neel P.
Bhatt <npbhatt@utexas.edu>.

Preprint. Copyright 2025 by the authors.

ming languages (Yang et al., 2025; Li et al., 2024; Yang
et al., 2024b). Although these methods offer guarantees, the
logical specifications they rely on have limited expressive-
ness and demand domain expertise, limiting their accessibil-
ity. On the other hand, several works query language models
for compliance classification, allowing inference with natu-
ral language rules (Guan et al., 2024; Hassani, 2024; Ying
& Sacks, 2024). Still, they can suffer from misclassifica-
tions due to the black-box nature of language models, hence
limiting their reliability.

This work addresses the challenge of predicting rule compli-
ance in planning tasks, where interpretibility and reliability
are essential, but often in tension. We consider Al systems
that generate and execute a plan, i.e., a sequence of actions,
subject to a set of constraints. These constraints may be
combinations of logical specifications and natural language
rules. While logical specifications support formal verifica-
tion through tools such as model checkers (Pnueli, 1977;
Baier & Katoen, 2008), they require structured input and
domain expertise. Conversely, natural language rules are
easy to obtain and express but lack the semantics required
for formal verification.

We introduce RepV, a neurosymbolic verifier that bridges
the gap between the two means of expressing constraints


RepV: Scalable Neurosymbolic Verification

." Interpreter’s prediction -
y" € {0,1} with rationale :

Unsafe: wrong Interpretation y # y*

: H H H : Interpreter’s prediction
: anjy : : Interpreter (LLM) : ty € {0,1} with rationale % : wo. 1
: : Model checking result ~~: J o .

Safe: correct interpretation y = y*

: Latent Space
R KB A vector space preserving the information
: of the plan, specs, and rationale: p, ®, #

Safe/Unsafe: y' = y* ?

Figure 1. A high-level illustration of RepV. It first learns a latent space where the safe (the interpreter’s prediction aligns with the model
checker) and unsafe plan representations are separable. Then, it classifies the new plan’s compliance against the new natural language
rules based on the spatial location of the plan representation in the latent space.

and verifies whether a plan complies with constraints, dur-
ing system execution. RepV begins with a modest set of
plans labeled via formal verification tools. It then trains
a lightweight projector that maps each plan, along with
an LLM-generated rationale, into a low-dimensional latent
space. Within this space, each point represents the align-
ment between the interpreter’s predicted compliance and the
formal verification outcome. We define a plan as safe when
this alignment holds, i.e. when the interpreter’s prediction
matches the verification result. Once the projector is trained,
RepV classifies compliance with unseen natural language
rules based on a plan’s spatial location in the latent space.
We present an illustration of RepV in Figure 1.

Building on the alignment-based latent space, we establish
a probabilistic guarantee (statistically calibrated probability
estimate) on RepV’s correctness of verification. Instead
of producing a binary label, the verifier quantifies the like-
lihood that its inferred compliance outcome aligns with
the formal verification outcome. This likelihood is cali-
brated using the distance between the generated plan and
the cluster centroid of a correctly aligned representation in
latent space. Rather than providing a binary assessment
of the plan, RepV offers a calibrated confidence estimate,
enabling uncertainty-aware compliance assessments.

We further leverage the probabilistic guarantee to guide re-
finement of the external planner (multimodal foundation
model) in a data-efficient manner. We propose two ap-
proaches for guarantee-driven refinement: Supervised fine-
tuning and preference optimization. The former identifies
high-guarantee compliant plans and uses them to fine-tune
the planner’s parameters, consolidating reliable planning be-
haviors. The latter ranks data preferences via the probabilis-
tic guarantee and train the planner to produce the preferred
planning behaviors. Together, the guarantee-driven refine-
ment forms a lightweight feedback loop that continuously
improves the plan-generation model without additional hu-
man labeling or full retraining.

Empirical evaluations show that RepV improves compli-
ance prediction accuracy by up to 15% compared to base-

line methods while adding fewer than 0.2 M parameters.
The evaluations are over four domains: simulated driving,
outdoor navigation (i.e., real-world driving), indoor navi-
gation, and 3D aerial navigation. RepV achieves over 90%
prediction accuracy in all domains, showcasing its reliabil-
ity in safety-critical domains and generalizability to unseen
environments and rules.

Furthermore, the guarantee-driven refinement improves the
planner’s ability to generate rule-compliant plans and outper-
forms ordinary fine-tuning baselines. Across four robotic
domains, this refinement process improves the planner’s
plan-generation compliance rate by up to 15% while re-
ducing convergence time by more than half compared to
ordinary supervised fine-tuning. The results demonstrate
that probabilistic guarantees provide an efficient mechanism
for continual planner improvement, transforming verifica-
tion feedback into actionable learning signals.

Contributions: We develop a neurosymbolic verifier,
RepV, that enables accessible and reliable planning through
three key contributions: (I) RepV enables compliance veri-
fication with natural language rules, eliminating the need to
handcraft logical specifications. (2) It establishes a proba-
bilistic guarantee that quantifies the likelihood of each com-
pliance judgment, enabling uncertainty-aware verification
with natural-language rules. (3) The probabilistic guarantee
enables continual improvement of external plan generation
models with minimal overhead.

2 RELATED WORK

Formal Methods for Compliance Verification: Formal
methods offer rigorous guarantees for verifying system
behavior against well-defined specifications (Mehdipour
et al., 2023). Techniques such as model checking have been
widely applied in safety-critical systems (Baier & Katoen,
2008). Recent works have expanded these techniques to
support more input modalities (Bhatt et al., 2024), such as
programming languages and natural language, by translat-
ing them into structured (mathematical) representations (Li


RepV: Scalable Neurosymbolic Verification

et al., 2024; Yang et al., 2024b;a). While compelling in
well-defined domains, these techniques struggle with the
ambiguity and variability of rules expressed in natural lan-
guage.

Language Models for Compliance Checking: Recent
advancements in language models with strong reasoning
capabilities enable new approaches to compliance checking
(Guan et al., 2024; Ying & Sacks, 2024; Gan, 2024). These
models extract obligations and detect non-compliance in
legal, financial, and healthcare texts, outperforming formal
methods approaches in flexibility and scalability (Hafizi,
2024; Hassani, 2024; Hassani et al., 2024; Berger et al.,
2023). While these approaches effectively enhance model
safety, lacking formal guarantees can be unreliable, espe-
cially in safety-critical domains.

Neurosymbolic Approaches for Compliance Checking:
Several studies integrate learned symbolic representations
of behaviors from logic-based systems to assess whether the
behaviors comply with well-defined rules (Barbara et al.,
2023; Pakina & Pujari, 2024; Paul, 2025; Ahn et al., 2025;
Bhatt et al., 2025b; Daggitt et al., 2024). Despite promising
results, they require handcrafted logic-based systems or are
limited to structured domains. Hence, they lack scalabil-
ity towards long-term development. Additionally, several
works use LLM-based modules to transform informal con-
straints into formal logic (Ganguly et al., 2024; Lee et al.,
2025), improving scalability. However, the reliability of this
transformation is not guaranteed due to the black box LLM.

Latent Signals from LLM Activations: Recent stud-
ies have revealed that LLMs encode rich internal signals
in their activations (intermediate outputs), such as indi-
cators of errors (Orgad et al., 2024; Karnik & Bansal,
2025; Bhatt et al., 2025a), hallucinations (Ferrando et al.,
2024), refusals (Arditi et al., 2024), and overthinking ten-
dencies (Chen et al., 2025). They often capture subtle model
behaviors that are not directly observable from the output
text. Inspired by these findings, we explore whether safety-
related signals can be detected within the model’s encoded
activations.

3 PRELIMINARIES

Formal Methods in Sequential Decision-Making: Se-
quential decision-making refers to the process by which an
agent selects actions over time, where each action affects
future states. In this work, we focus on planning problems
which are a subset of sequential decision-making problems.
Given a planning task, we define a plan as a sequence of
actions that achieve a task goal. In this work, we describe a
plan in either natural language or programming language.

Formal methods provide tools for modeling and verifying
planning problems represented mathematically. Suppose we

a —g

0 start B~
True True Oro

Figure 2. Automaton corresponding to a syntax “if o, do wi, else
wa” (left) and “while o, do wz” (right).

have a transition system T'S modeling the world knowledge
or autonomous systems, a finite-state automaton (FSA) A
representing the plan, and a temporal logic specification
¢ constraining the temporal ordering and logical relations
between actions. A and T'S are mathematical structures
with a finite number of states (Baier & Katoen, 2008), and ¢
is a logical formula with temporal operators such as “always”
and “eventually” (Pnueli, 1977).

We apply a model checker, a tool from formal methods, to
verify whether 4, when implemented in T'S, satisfies ¢,
denoted as A® T'S |= ¢ (Baier & Katoen, 2008; Cimatti
et al., 2002). We present the formal definitions of these
terminologies in Appendix A.

Text-Based Plan to Automaton: To formally verify a plan
against logic specifications, we need to express the plan
in a formal representation such as an FSA. Existing works
have developed methods for converting natural language or
programming language to FSA (Yang et al., 2025; 2024b).

We use an algorithm, denoted as L2A, that takes a text-
based plan p, expressed in natural language or programming
language, as input and converts it into an FSA A = L2A(p).
In particular, the algorithm parses the text input into phrases
(e.g., keywords and variables in a programming language).
It then follows pre-defined grammar to convert the parsed
phrases into automaton states and transitions. We present
an automaton conversion example in Figure 2 and present
the details in Appendix B.

4 REPV: A NEUROSYMBOLIC VERIFIER

RepV is a neurosymbolic verifier that unifies formal verifi-
cation with representation learning to enable verification
of externally generated action plans against natural lan-
guage rules. At its core, RepV constructs a latent space
where safe and unsafe plans are linearly separable, enabling
efficient inference of compliance with natural-language
rules. This latent-space embedding bridges the symbolic
rigor with linguistic accessibility, establishing a foundation
to extend verification toward probabilistic guarantees and
iterative refinement.

Components: RepV comprises four main components:
an external knowledge source for plan generation, an inter-
preter M1, a projector P, and a model checker Mc.

- Knowledge Source (Planner): An external source used


RepV: Scalable Neurosymbolic Verification

External Knowledge Source
Plan

L2A

¢ = G( pedestrian v car) — X- move

Logical Specification ¢

y*e {0,1}
— Training Data Collection

— Projector Training

— Compliance Inference

Projector Training Data D;,

{input: (z, y), label: y*} Supervised

EY a Projector

New Natural
Language Rule 77,

i AR’ = “The plan successfully... i
but violates......” f

H {Interpreter

0~fail, 1~pass

“Wait for pedestrian,
then turn right”

00 00

r,; = Yield at pedestrians
and cars.

Natural Language Rule 7,

Wrong Interpretation (Unsafe)

Compliance
Prediction

y€ {0.1}

Latent Space (prior training)
Star: z (representation)

Wrong Interpretation (Unsafe)

ted

Correct Interpretation (Safe)
Latent Space (after training)

Figure 3. An illustration for collection of auto-labeled training data for the projector, training the projector to construct a safety-separable
latent space, and predicting plan compliance against natural language rules via the spatial location of the projector’s output representation.

for plan generation. In this setting, we use a multimodal
foundation model that accepts text- or image-based task
prompts as input, generating a text-based plan that fulfills
the task. Note that the rule is a part of the task prompt as
well. For brevity, we denote it as a planner.

- Interpreter: A language model M7 that processes a gen-
erated plan p and a natural language rule r,,;, producing a
binary classification y € {0, 1} indicating its prediction on
rule compliance (1 means comply) and a text-based ratio-
nale R. Mathematically, we have y, R = Mz(p, ni). An
illustrative example of interpreter outputs is in Section 6.

- Projector: A multi-layer perception P : R® — R™ that
takes the e-dimensional text embeddings emb(p, R) € R€
derived from the plan and the interpreter’s rationale and
maps them into an m-dimensional latent space, where com-
pliance and noncompliance can be distinguished spatially.

- Model Checker: A formal verification tool M verifies
whether a plan p, represented as a finite-state automaton
A, = L2A(p), satisfies a logical specification ¢ when ex-
ecuted in a world/system model 7'S. It returns a binary
ground-truth label indicating compliance:

Mc(Ap, 9) =1if A, @ TS |= ¢, else 0.

Definition of Safety: To learn a safety-separable latent
space, we first define the terminology on safe and unsafe.

Definition 1: Given a plan p, a natural language rule r,,,
and a logical specification ¢ that corresponds to r,; (i.e., 7;
describes the semantic meaning of ¢), we obtain a natural
language interpretation y, R = Mz(p, r,;) and the formal

verification outcome y* = Mc (Ap, 9).

We define the plan p as safe with respect to the rule 7, if
and only if its predicted classification aligns with the formal
verification outcome:

pissafe <— y=y". (DH

Note that the “safety” in RepV differs from the everyday
notion of physical or behavioral safety. A plan is consid-
ered safe even if it violates the rule (y* = 0), as long as
the interpreter’s prediction correctly reflects that violation
(y = 0). Conversely, a plan is unsafe when there is a
misalignment between the interpreter’s prediction and the
verified outcome (y # y*), regardless of whether the plan
itself satisfies or violates the rule. Hence, safety in this
context measures semantic consistency between reasoning
modalities—the alignment of linguistic interpretation and
formal verification.

Problem Statement: Given a plan p extracted from ex-
ternal sources (e.g., Al-generated) and a natural language
rule r,;, verify whether each plan complies with the rule,
denoted as p |= r,;, and estimate the probability of such
verification outcome being correct.

4.1 Learning a Safety-Separable Latent Space

To achieve reliable verification across linguistic and formal
modalities, RepV learns a latent space where semantically
safe and unsafe plans become linearly separable. This latent
space serves as a bridge between linguistic reasoning (from
Mz) and symbolic verification (from M¢), enabling the


RepV: Scalable Neurosymbolic Verification

system to infer compliance for new natural-language rules
based on spatial proximity.

Training Data Collection: We derive each training sample
from a triplet (p, rn1, ¢), where p is a generated plan, 7, is
a natural-language rule, and ¢ is its corresponding logical
specification. The interpreter M7 predicts a compliance
label y and generates a rationale R:

We then convert the plan p into an automaton A, = L2A(p)
and verify it against ¢ by the model checker M, producing
a ground-truth label y*:

y= Mc(Ap, 9). (©)

Latent Representation: We obtain a text embedding
emb(p, R) € Rc using a pretrained text encoder emb()
that encodes the plan p and the interpreter’s rationale R.

A lightweight projector P : R® — R™ maps this em-
bedding into a low-dimensional latent representation z =
P(emb(p, R)). Each representation z is associated with a
binary label y,,. that indicates whether the interpreter’s
prediction aligns with the verified outcome:

(safe),
(unsafe).

Ysare = 1, y=y*
safe — *
0, y#vy

Projector Optimization: We then train the projector using
a cross-entropy loss that minimizes the classification error
between the predicted safety §,. se (predicted via the spatial
location in the latent space) and the verified label y, f.:

“4

Lproj == > yo log gay) log(1-3%..).

Through supervised learning, RepV learns a safety-
separable latent space Z C R™, where spatial clusters
encode alignment between linguistic interpretation and for-
mal verification. As shown in Figure 3, the process involves
collecting formally verified samples, projecting them into
Z, and learning a linear boundary that partitions safe and
unsafe representations.

4.2 Probabilistic Rule Verification

After obtaining the latent space Z, RepV infers compliance
for new plans and rules by projecting them into Z and esti-
mating a probabilistic guarantee that reflects the probability
of correct verification.

Inference Procedure: Given a new plan p’ and a natural
language rule r,;, the interpreter M7 produces a binary
classification and rationale:

y,R' = Mz (p, ni)»

and the projector maps the text embedding into the latent
space:
2 =P(emb(p/,R)).

We then attach a linear classifier C' : R™ — {0,1} to
predict the compliance label:

safe = (2) € {0, 1},

where 1 indicates that the interpreter’s reasoning is aligned
with the expected formal outcome.

Calibration Dataset: To provide a probability estimate
for this inference, we maintain a small calibration set
De = {(zi, vi, y])} that is i.i.d. with the potential testing
set, where each z; = P(emb(p;, R;)) is a latent representa-
tion and y; is the formal verification result from M.. For
each correctly classified sample (y; = vy), we compute its
distance to the centroid c* of the corresponding cluster:

1

z;€S

where S is the set of correctly aligned (safe) samples. These
distances form a calibration distribution Fo (d) that captures
how far safe samples typically lie from their centroid.

Distance-Based Probabilistic Guarantee: For a new in-
ference (p', r!,) with latent representation z’, we compute
its distance to the nearest centroid. We then formulate a
calibration distribution that captures the relation between
the distances and prediction accuracies. This distribution
is used to estimate the probability that a prediction y’ is
correct, denoted as the probabilistic guarantee p(y’ | 2’).

Definition 2 (Distance to Centroid): Let c, and c,, be the
centroids of the safe and unsafe clusters in Z. For a new
input (p/,77,) with embedding 2’, the distance to its nearest
centroid is

d = min (II —csll2, 12 — cull2)-

Definition 3 (Calibration Distribution): Let Fc : RT —
[0, 1] denote the cumulative distribution of distances from
correctly classified samples to their cluster centroids. Let
ys € {0,1} be the safety label associated with ¢*. For a
new input, Fo (d') gives the probability that a sample from
the opposite class lies beyond distance d’:

Fo(d') = Pr [|lzi = ¢"ll2 > d'| ys # vz].

If the safe and unsafe classes are imbalanced, we compute
Fo separately for each class and select the corresponding
F based on the nearest centroid.

Theorem 1 (Probabilistic Guarantee): Given a new in-
put with latent representation z’ and nearest centroid ¢* €


RepV: Scalable Neurosymbolic Verification

Wrong Interpretation (Unsafe)

HOD
@ ees » Representation z

. ) A | SEELEELIEL CE » Centroid c*
w FN

Correct Interpretation (Safe) !
Latent Space (after training) Distance to centroid

d=|lz=c*|l,
| Calibration Data D
{input: (z, y), label: y*}

— Data Calibration

Calibration Distribution F

Guarantee-guided

— Probabilistic Guarantee

Latent Space |
Probabilistic Guarantee
PO*| 2)

Guarantee-guided

data filtering preference ranking

| Fine-Tuning Data Dy,
{ task prompt, plan }

— Guarantee-Guided Refinement

[SEGE
Knowledge
Source

SFT or DPO

Figure 4. Overview of the probabilistic verification and refinement pipeline. Left: After training the latent space, we extract a calibration
distribution Fc using distances from correctly verified samples to their cluster centroids. Right: Given a new plan—rule pair, we compute
the distance between its latent representation and the nearest centroid ¢*. Then, we use Fc to compute the probabilistic guarantee. The
guarantee can be subsequently used as a feedback signal for upgrading the planner.

{¢s, cu }, the probabilistic guarantee p(y’ | 2’) that its clas-
sification is correct is
A 1 = Fo(d)) - Pry # yz]
/ / — 1 ( T Cc .
Hy | #) Plz — c=

6)

Proof 1: Let ¢* € {cs, c,} be the nearest centroid of the
new input’s latent representation 2’, and let d’ = ||z’ — ¢*||2.
By Definition 3, the calibration distribution satisfies

Fo(d) = Pr[l|lzi — "lla > d' | yi # v2].
We define the probabilistic guarantee as
py | 2) =Prly; = yi | lz — "lla < d]
=1=Prly # yc | lz = ll < d'].
Applying Bayes’ rule,
Pry; # y | llzi — [la < d']

_Prflzi —ctlls < dT yi # ye [Priyi # ye]
Prf|z; — ella < d']

(1 — Fo(d)) Prly: # yz]

Pr(flz; — cll <d']

Substituting into the definition of p(y’ | 2’), we get

(1—Fo(d)) Pry: # yz]
Pr[|[zi — c*|]2 < d']

ply 2) =1
Hence proven.

Verification: p(y’ | 2’) denotes uncertainty associated with
the rule compliance prediction. A higher p(y’ | 2’) implies
stronger alignment between linguistic interpretation and
formal verification. Formally, a new plan p’ complies with
a natural-language rule r/,; with a guarantee p(y’ | 2) if

PE =
(Frage =1AY =1) Vv (Yoase =0AY =0)]. (6)

5 GUARANTEE-DRIVEN REFINEMENT

To close the loop between verification and planning, RepV
uses the probabilistic guarantee p(y | z) as a feedback
signal to refine the planner, which is a foundation model that
produces plans. We adopt two complementary strategies:
supervised fine-tuning and preference optimization.

Supervised Fine-Tuning (SFT) via Guarantee Filtering:
We collect a dataset of plan—rule pairs (p, r,,;) along with
their interpreter predictions and latent probabilities. We
then filter this dataset to retain only high-confidence exam-
ples, i.e. those where the probabilistic guarantee exceeds a
threshold 7 (e.g. 0.9). Formally, define

Dit = { (prt) : 0(y | 2) > 7}.

We use Dy to fine-tune the planner via cross-entropy loss,
training it to generate plans that are rule compliant. Because
the filtered set excludes high-uncertainty examples, the fine-
tuning process focuses on reinforcing desired patterns.

Probabilistic Guarantee for Preference Ranking: We can
use the probabilistic guarantee p(y | z) also as a measure of
relative preference between alternative plans generated by
the planner.

For each given task, the planner generates two different
plans p; and py by varying the random seed. Then, we
estimate the guarantee for both plans: p(y; | 21) and p(y, |
22). We select the plan with higher guarantee of rule
compliance as the preferred output, and the other as the non-
preferred output. Lastly, we add this triplet (task prompt,
preferred output, non-preferred output) into the fine-tuning
dataset Dy.

We then directly pass Dj, into a Direct Preference Optimiza-
tion (DPO) framework to fine-tune the planner. Compared


RepV: Scalable Neurosymbolic Verification

Figure 5. Example visual and textual input prompts to the planner.

to supervised fine-tuning, preference optimization performs
better when multiple correct answers exist.

6 DEMONSTRATION

We demonstrate RepV on navigation tasks using the Carla
simulator (Dosovitskiy et al., 2017). In this section, we (1)
present our experimental setting, (2) visualize the safety-
separable latent space and its corresponding calibration dis-
tribution, and (3) present case studies that highlight its veri-
fication capability.

Experimental Setup: We design a series of naviga-
tion tasks, such as go straight, turn left, turn right, park,
and make a U-turn at the traffic light or the stop sign
intersection. We use GPT-ol-mini as the interpreter
and a NuSMV model checker (Cimatti et al., 2002). The
projector comprises of three fully connected layers that
map 1536-dimensional text embeddings (obtained from
text-embedding-3-small) to a 10-dimensional la-
tent space (m = 10).

Given a set of pre-implemented APIs, such as:

I| # Publishes linear and angular velocity
to the agent.

2 velocity_publisher (linear,

3 stop () # Stops the robot.

i # Returns a boolean value.

5 pedestrian_observed ()

angular)

we use GPT-4.1-mini as our planner to generate high-
level plans composed of sequences of the system APIs.
Specifically, the planner takes a text-based and/or image-
based task prompt and the system APIs as inputs, producing
a high-level plan, i.e., a Python program, that calls the APIs
to accomplish the task. We show examples of input prompts
in Figure 5 and output plans in Listing 1.

Training: We generate 400 plans and 15 natural-language
rules, each paired with its formal logic specification, to train
the projector. For example, the rule “Yield to pedestrian’
corresponds to

>

¢1 = G pedestrian — X— publish velocity.

The complete rules are in Listing 2 (Appendix C.3).

For each plan, we randomly sample one rule from the 15-
rule set and query the interpreter to predict compliance and

provide an explanation. Then, we use the model checker
to obtain the ground-truth compliance label by verifying
the corresponding formal specification. Each compliance
prediction and rationale generation by the interpreter takes
less than 3 seconds, while the model checking requires less
than 0.1 seconds per plan.

After collecting 400 labeled samples, we train the projector,
which contains 197 K parameters, to predict rule com-
pliance using cross-entropy loss with a batch size of 20
over 10 epochs. The full training process completes in 144
milliseconds. Figure 6 shows the latent spaces before and
after training, demonstrating how the learned space achieves
linear separability between safe and unsafe plans.

Calibration: We construct a calibration set of 400 ad-
ditional samples drawn from the same distribution as the
training set, using the same set of natural-language rules
and their corresponding formal specifications. Each sample
is passed through the trained projector to obtain its latent
representation and distance to the nearest cluster centroid.
These distances form the calibration distribution Fo. This
calibration process completes in only 225 milliseconds. We
show the resulting distribution in the right plot of Figure 6.

Compliance Verification on Unseen Plans: Once the la-
tent space and calibration distribution are established, we
apply RepV to verify a new plan (Listing 1) against a new
natural-language rule: Always avoiding pedestrians. This
rule conveys the same underlying constraint as the training
rules but is phrased differently, testing the model’s ability to
generalize to paraphrased expressions of compliance.

I def go_across_the_intersection() :

while dist < 20.0:
A if pedestrian_observed() :

5 stop ()

6 else:

7 velocity_publisher (speed, angle)
8 dist = speed x (time.time() - t)

Listing 1. A plan for the task: “go across this intersection.”

Given the meta prompt to the interpreter (including the rule):

“Does the code meet the rule: Always avoiding pedestrians?”
the interpreter classifies the plan as violating the rule (3 =
0) with an explanation: “It partially meets the rule. It stops
when a pedestrian is detected during turning and completes
the turn if unobstructed.”

Then, we obtain the latent representation of this plan z’.
The distances of z’ to the centroids of the safe and unsafe
clusters are [1.2517, 1.0975], indicating that the sample lies
closer to the unsafe cluster. The corresponding probabilistic
guarantee is p(y’ | 2’) = 0.971, suggesting that RepV is


RepV: Scalable Neurosymbolic Verification

Latent Space Visualization (Before Training) Latent Space Visualization (After Training)

Latent Space Visualization (Testing) Calibration Distribution Fc

JTS SR —
rae,
I *
*.

© “

0 LEY

% Other-Class Points Beyond d

‘ \
[3 secsssscoestosases

12 14 26 28

«Correct Interpretation «Incorrect Interpretation «Correct Interpretation «Incorrect Interpretation

18 20 22 24
Distance d to Centroid

«Correct Interpretation «Incorrect Interpretation Incorrect Interpretation = Correct Interpretation

Figure 6. The figures from left to right show: 1) the latent space before projector training, where safe and unsafe plans are inseparable, 2)
latent space after training, which is separable by a linear decision boundary, 3) representations of out-of-domain testing samples with new
rules, and 4) calibration distribution Fc derived from a calibration set that drawn from the same distribution with the training samples.

obstacle

Figure 7. Automaton-based representations of the plans for a Carla
simulated driving task (left) and an indoor navigation task (right).

confident that the interpreter’s prediction 3’ is incorrect.
This inference procedure takes an average of 2.5 seconds.

Hence, we conclude that the plan has a 97.1% chance of
satisfying the rule. To validate this conclusion, we convert
the plan into an automaton (left in Figure 7) and verify
it against ¢;. The model checker confirms that the plan
satisfies the specification.

This case highlights how RepV detects interpreter misclassi-
fications. By leveraging the geometric separation and quan-
tifying uncertainty in the latent space, RepV can rectify
the language model interpreter’s misclassification, demon-
strating its ability to provide reliable, uncertainty-aware
verification against natural-language rules.

7 QUANTITATIVE ANALYSIS

We quantitatively evaluate RepV across multiple robotic
platforms to assess its verification accuracy and impact on
downstream planning performance, e.g., how the verifica-
tion outcome guides the planner refinement.

7.1 Rule Compliance Prediction

We evaluate the compliance prediction accuracy of RepV
across four application domains and compare it against two
baseline methods, as shown in Figure 8. Given a set of plans
and a set of rules, we define compliance accuracy as

|
Accuracy = N > Ly: =v], ©)
i=1

Unitree Go2
mm NN Classifier

PX4 Drone
mam RepV

Carla Jackal
mem LLM Reasoning

Figure 8. Comparison of three rule compliance prediction methods
over four robot planning domains. RepV consistently achieves the
highest accuracy compared to the two baselines. The error bar
shows the standard deviation across five repetitive runs.

where N is the number of testing samples, y; denotes the
predicted compliance label, y; the ground-truth label from
model checking, and I[-] the indicator function.

Baselines: We compare our method against two baseline
methods listed below.

(1) LLM Reasoning: We follow the framework pro-
posed by (Guan et al., 2024), using a foundation model
(GPT-4.1-nano) directly classifies plan compliance from
text inputs via chain-of-thought reasoning.

(2) NN Classifier: a 10-layer perception trained with 400
samples for 10 epochs, taking plan embeddings as input to
predict compliance.

(3) RepV (ours): the proposed neurosymbolic verifier, which
infers compliance according to Equation 6.

Domains: We evaluate the baselines over four robot plan-
ning domains of increasing embodiment diversity: Carla
driving simulator, Jackal ground robot sharing identical
APIs and tasks with Carla, Unitree Go2 legged robot
we used for indoor navigation, and a PX4 Vision 1.0
quadcopter (drone) we used for 3D aerial navigation. Each
domain contains 40 generated plans and 5 natural language
rules, where each plan is checked against 5 rules separately.
We present the rules in Appendix D.1.

Results: RepV consistently achieves the highest compli-
ance accuracy across all domains, maintaining at least 95%
compliance accuracy on in-domain tasks (Carla and Jackal)


RepV: Scalable Neurosymbolic Verification

Table 1. Fine-tuning Statistics. Acronyms: BS = Batch Size, LR = Learning Rate Multiplier, E = Epochs, TD = Training Duration (min),
TT = Trained Tokens, DS = Data Size (KB), S = Number of Samples, S/E = Steps per Epoch, Steps = Total Steps, CE = Convergence
Epoch, CStep = Convergence Step. We define convergence if the training compliance rate fluctuates by less than 1% in the later
epochs/steps. Our fine-tuning strategies require fewer training samples and converge faster than the baselines.

Fine-Tuned Model BS LR E TD TT DS S S/E Steps CE CStep
SFT (No Filter) 2 003 5 1697 838,630 658.55 400 200 1000 5 1000
SFT via Confidence Filter 2 003 5 7440 330,225 257.11 150 75 375 4 270
SFT via Guarantee Filter (Ours) 2 0.03 5 8.38 346,670 285.00 150 75 375 2 175
DPO (Ours) 2 003 5 50.68 370,760 233.22 200 100 500 2 120
£2 1.0 5 10 —e— Testing
oc oc
9 0.9 © 0.9
5 £ T=
ge H 0.8
©o0.7 S 0.7
Carla Jackal Unitree Go2 PX4 Drone 06 07 08 09
msm GPT-4.1-nano (raw) mmm SFT (no filter) mmm SFT (confidence filter) mmm SFT (guarantee filter) mmm DFO | Guarantee Threshold

Figure 9. The left figure compares compliance rates across various refinement methods. The right figure shows the testing performance of
our guarantee-filtered SFT at different guarantee thresholds, in which achieves the best the performance at threshold 0.8. The error bar
shows the standard deviation across three repetitive runs. Our methods (red and purple) outperforms the other baselines in all robotic

domains and achieves higher performance gain in out-of-domain tasks (Go2 and PX4 drone).

and 90% accuracy on out-of-domain tasks (Go2 and Drone).
In contrast, the two baselines exhibit significant degradation
when transferred to new domains, highlighting the robust-
ness of our verifier.

Figure 6 (the third from left) visualizes the latent-space dis-
tributions of out-of-domain tasks (Go2 and Drone). Notably,
these out-of-domain representations exhibit approximately
identical distributions as those trained on Carla, showing
that the latent space primarily captures the interpreter’s rea-
soning behavior rather than domain-specific information.
Because the interpreter’s linguistic reasoning pattern re-
mains consistent across various domains, the latent geom-
etry of safe versus unsafe plans remains stable, enabling
robust cross-domain transfer without retraining.

7.2 Planner Refinement

After demonstrating RepV’s capability in rule compliance
prediction, we leverage its verification results to refine the
knowledge-source planner. This section demonstrates how
our guarantee-driven refinement effectively and efficiently
enhances the planner to generate rule-compliant plans.

For quantitative analysis, we compare our guarantee-driven
refinement methods against several baselines:

SFT (No Filter): Standard supervised fine-tuning using all
foundation model-generated plan—rule pairs, regardless of
their verification confidence.

SFT via Confidence Filter: Fine-tuning performed only
on plan—rule pairs whose interpreter assigns a high softmax

confidence score (> 0.8) to its compliance prediction.

SFT via Guarantee Filter (Ours): Fine-tuning restricted
to samples satisfying the rules with probabilistic guarantee
p(y | 2) > 7 = 0.8. We present the performance over
different guarantee values in Figure 9 (right).

DPO (Ours): DPO using the probabilistic guarantee as a
ranking signal between alternative plans.

To evaluate their performance, we define the metric compli-
ance rate as

®)

= Tl

N
1
compliance rate = N > I [pi
i=1

where NV is the number of testing plans, p; denotes the i-th
generated plan, r,,;; the corresponding natural-language
rule, and I[-] = 1 if and only if the plan is rule-compliant.
This metric measures the proportion of generated plans that
formally satisfy the given rules.

We present the fine-tuning details in Table 1 and evaluate
the fine-tuned planners on 160 test samples, each labeled by
the model checker using handcrafted formal specifications.
The evaluation covers four domains: the Carla simulator
(training environment, in-domain), the Jackal ground robot
(real-world counterpart of Carla, sim2real transfer), the Uni-
tree Go2 (out-of-domain), and the PX4 (out-of-domain). We
present the rules for each domain in Appendix D.1.

Figure 9 shows the compliance rates achieved by each plan-
ner. Across all domains, both of our fine-tuned models
outperform the baselines by 10% to 20% in compliance


RepV: Scalable Neurosymbolic Verification

pedestrian=car=False
publish velocity (turn left)

pedestrian=False, car=1ruc
stop

pedestrian=car=False
publish velocity (move forward)

(third-person view) obstacle=False

(third-person view) obstacle=False

(third-person view) obstacle= ruc
step right

move forward

[|
(first-person view) obstacle=1ruc
step right

(third-person view) obstacle=False

turn right move forward & sit down

Figure 10. We execute the plan generated by our fine-tuned planner on real robots for outdoor and indoor navigation. The robots complete

the task while complying with the domain-specific rules.

Task Prompt: Fly in a square-shaped trajectory.

Take off
set_velocity_ned(0,0,-0.5)

Move east
set_velocity_ned(0,1,0)

Trajectory

Land
Weather: north wind set_velocity_ned(0,0,0.5)

Figure 11. The fine-tuned planner generates executable plans that
complies rules in aerial navigation.

rate, and the performance gap widens in out-of-domain
tasks. Meanwhile, our fine-tuning strategies halve the num-
ber of training samples and the convergence time. These
results highlight that using RepV’s probabilistic guarantee
as feedback enables data-efficient, safety-aligned planner re-
finement and generalization across different tasks and rules.

7.3 Real-World Deployment

We further validate the refined planners (guarantee-filtered)
in real-world environments to examine whether RepV can
guide safe and executable behaviors. We present the com-
plete task prompts and executable plans in Appendix D.2.

Outdoor and Indoor Navigation: Figure 10 shows the
execution of the generated and RepV-verified plans on two
physical platforms. All the executed plans are verified as
compliant with a probabilistic guarantee of 90%+.

For the Jackal ground robot, we specify a rule “Yield to
pedestrians and coming vehicles.” The robot crosses the
intersection and crosswalk while dynamically adjusting its
actions to give right of way to pedestrians and nearby cars.

For the Unitree Go2, we specify a rule that is distinct from
outdoor navigation: “Bypass the obstacles; do not stop.
The resulting behavior, as presented in Figure 7 (right),
demonstrates that the planner generalizes to different loco-
motion modalities and physical dynamics, achieving reliable
obstacle avoidance without halting unnecessarily.

>

Aerial Navigation: Figure 11 extends this evaluation to
a 3D aerial navigation task. The rule is: “Keep altitude
below 10 meters and landing speed within 1 m/s.” The
drone follows a high-level plan that draws a square trajectory

under mild wind disturbance while respecting altitude and
landing-speed constraints.

Summary: The RepV-refined planners successfully pro-
duce executable and rule-compliant plans in various physi-
cal environments, enabling platform agnostic and safety-
constrained plan generation and verification across dif-
ferent robot embodiments and operational constraints.

8 CONCLUSION

We introduce RepV, a neurosymbolic verifier that learns
a safety-separable latent space bridging the rigor of for-
mal verification with the accessibility of natural-language
reasoning. By embedding plans and interpreter rationales
into this latent representation, RepV enables probabilistic-
guaranteed compliance verification without handcrafted
logic specifications.

Across simulated and real-world robotic applications, RepV
achieves over 90% compliance verification accuracy. Fur-
thermore, we leverage the probabilistic verification outcome
to guide planner refinement, improving compliance rates of
the generated plans by 10-20% while halving convergence
time during refinement.

Limitations and Future Work: Although RepV general-
izes across embodiments, the learned projector currently
requires retraining when transferred to distinct reasoning
domains beyond robotics and planning, such as medical or
finance. On the other hand, we show that the projector learn-
ing and calibration require less than one second, making
adaptation practical for new environments. Hence, the pri-
mary difficulty lies in projector training and calibration data
collection, which may require domain expertise to provide
formal or domain-specific constraints.

Moreover, RepV focuses on natural-language rules. Future
works will aim to verify multimodal constraints (visual traf-
fic signals, speed-limit signs, or spatial safety boundaries)
by integrating perception modules into the latent-space rea-
soning loop. Another direction is to apply RepV to other
sequential decision-making domains like logistics, health-
care, and financial compliance, where natural-language rules
work alongside formal specifications.


RepV: Scalable Neurosymbolic Verification

REFERENCES

Ahn, S., Choi, W., Lee, J., Park, J., and Woo, H. To-
wards reliable code-as-policies: A neuro-symbolic frame-
work for embodied task planning. arXiv preprint
arXiv:2510.21302, 2025.

Arditi, A., Obeso, O., Syed, A., Paleka, D., Panickssery,
N., Gurnee, W., and Nanda, N. Refusal in language
models is mediated by a single direction. arXiv preprint
arXiv:2406.11717, 2024.

Baier, C. and Katoen, J.-P. Principles of model checking.
MIT press, 2008.

Balakrishnan, A. Leveraging artificial intelligence for en-
hancing regulatory compliance in the financial sector. In-
ternational Journal of Computer Trends and Technology,

2024.

Barbara, V., Guarascio, M., Leone, N., Manco, G., Quarta,
A., Ricca, FE, and Ritacco, E. Neuro-symbolic ai for com-
pliance checking of electrical control panels. Theory and
Practice of Logic Programming, 23(4):748-764, 2023.

Berger, A., Hillebrand, L., Leonhard, D., Deufler, T.,
De Oliveira, T. B. F.,, Dilmaghani, T., Khaled, M., Kliem,
B., Loitz, R., Bauckhage, C., et al. Towards automated
regulatory compliance verification in financial auditing
with large language models. In 2023 IEEE International
Conference on Big Data (BigData), pp. 4626-4635. IEEE,
2023.

Bhatt, N. P,, Yang, Y., Siva, R., Milan, D., Topcu, U., and
Wang, Z. Know where you’re uncertain when planning
with multimodal foundation models: A formal framework.
arXiv preprint arXiv:2411.01639, 2024.

Bhatt, N. P, Li, P-h., Gupta, K., Siva, R., Milan, D., Hogue,
A. T., Chinchali, S. P., Fridovich-Keil, D., Wang, Z.,
and Topcu, U. Uncap: Uncertainty-guided planning us-
ing natural language communication for cooperative au-
tonomous vehicles. arXiv preprint arXiv:2510.12992,
2025a.

Bhatt, N. P., Yang, Y., Siva, R., Samineni, P., Milan, D.,
Wang, Z., and Topcu, U. VIn-zero: Rapid exploration and
cache-enabled neurosymbolic vision-language planning
for zero-shot transfer in robot navigation. arXiv preprint
arXiv:2509.18592, 2025b.

Chen, R., Zhang, Z., Hong, J., Kundu, S., and Wang, Z.
Seal: Steerable reasoning calibration of large language
models for free. arXiv preprint arXiv:2504.07986, 2025.

Cimatti, A., Clarke, E. M., Giunchiglia, E., Giunchiglia, F.,
Pistore, M., Roveri, M., Sebastiani, R., and Tacchella,
A. NuSMV 2: An opensource tool for symbolic model

checking. In Brinksma, E. and Larsen, K. G. (eds.),
Computer Aided Verification, volume 2404 of Lecture
Notes in Computer Science, pp. 359-364, Copenhagen,
Denmark, 2002. Springer.

Daggitt, M. L., Kokke, W., Atkey, R., Slusarz, N., Arn-
aboldi, L., and Komendantskaya, E. Vehicle: Bridging
the embedding gap in the verification of neuro-symbolic
programs. arXiv preprint arXiv:2401.06379, 2024.

Daram, S. Explainable ai in healthcare: Enhancing trust,
transparency, and ethical compliance in medical ai sys-
tems. International Journal of Al, BigData, Computa-
tional and Management Studies, 6(2):11-20, 2025.

Deshpande, A. Regulatory compliance and ai: Navigating
the legal and regulatory challenges of ai in finance. In
2024 International Conference on Knowledge Engineer-
ing and Communication Systems (ICKECS), volume 1,
pp- 1-5. IEEE, 2024.

Dosovitskiy, A., Ros, G., Codevilla, F., Lopez, A., and
Koltun, V. Carla: An open urban driving simulator. In
Conference on robot learning, pp. 1-16. PMLR, 2017.

Ferrando, J., Obeso, O., Rajamanoharan, S., and Nanda,
N. Do i know this entity? knowledge awareness
and hallucinations in language models. arXiv preprint
arXiv:2411.14257, 2024.

Gan, Z. Large language models empowering compliance
checks and report generation in auditing. World Journal
of Information Technology, pp. 35, 2024.

Ganguly, D., Iyengar, S., Chaudhary, V., and Kalyanaraman,
S. Proof of thought : Neurosymbolic program synthesis
allows robust and interpretable reasoning, 2024. URL
https://arxiv.org/abs/2409.17270.

Grosan, C., Abraham, A., Grosan, C., and Abraham, A.
Rule-based expert systems. Intelligent systems: A modern
approach, pp. 149-185, 2011.

Guan, M. Y., Joglekar, M., Wallace, E., Jain, S., Barak,
B., Helyar, A., Dias, R., Vallone, A., Ren, H., Wei, J,
etal. Deliberative alignment: Reasoning enables safer lan-
guage models. arXiv preprint arXiv:2412.16339, 2024.

Hafizi, A. Auditing healthcare claims through large-scale
nlp-based consistency and compliance checks. Interna-
tional Journal of Data Science, Big Data Analytics, and
Predictive Modeling, 14(9):30-43, 2024.

Hassani, S. Enhancing legal compliance and regulation anal-
ysis with large language models. In 2024 IEEE 32nd In-
ternational Requirements Engineering Conference (RE),

pp- 507-511. IEEE, 2024.


RepV: Scalable Neurosymbolic Verification

Hassani, S., Sabetzadeh, M., Amyot, D., and Liao, J. Re-
thinking legal compliance automation: Opportunities
with large language models. In 2024 IEEE 32nd Inter-
national Requirements Engineering Conference (RE), pp.

432-440. IEEE, 2024.

He, H., Gray, J., Cangelosi, A., Meng, Q., McGinnity, T. M.,
and Mehnen, J. The challenges and opportunities of
human-centered ai for trustworthy robots and autonomous

systems. IEEE Transactions on Cognitive and Develop-
mental Systems, 14(4):1398-1412, 2021.

Karnik, S. and Bansal, S. Preemptive detection and steering
of Ilm misalignment via latent reachability, 2025. URL
https://arxiv.org/abs/2509.21528.

Lee, C. P, Porfirio, D., Wang, X. J., Zhao, K. C., and Mutlu,
B. Veriplan: Integrating formal verification and llms
into end-user planning. In Proceedings of the 2025 CHI
Conference on Human Factors in Computing Systems,

CHI 25, pp. 1-19. ACM, April 2025. doi: 10.1145/

3706598.3714113. URL http://dx.doi.org/10.

1145/3706598.3714113.

Li, Z., Hua, W., Wang, H., Zhu, H., and Zhang, Y.
Formal-llm: Integrating formal language and natural lan-
guage for controllable llm-based agents. arXiv preprint
arXiv:2402.00798, 2024.

Mehdipour, N., Althoff, M., Tebbens, R. D., and Belta,
C. Formal methods to comply with rules of the road in
autonomous driving: State of the art and grand challenges.
Automatica, 152:110692, 2023.

Orgad, H., Toker, M., Gekhman, Z., Reichart, R., Szpek-
tor, I., Kotek, H., and Belinkov, Y. Llms know more
than they show: On the intrinsic representation of llm
hallucinations. arXiv preprint arXiv:2410.02707, 2024.

Osifowokan, A. S., Agbadamasi, T. O., Adukpo, T. K., and
Mensah, N. Regulatory and legal challenges of artificial
intelligence in the us healthcare system: Liability, com-
pliance, and patient safety. World Journal of Advanced
Research and Reviews, 25(3):949-955, 2025.

Pakina, A. K. and Pujari, M. Neuro-symbolic compliance
architectures: Real-time detection of evolving financial
crimes using hybrid ai. International Journal Science

and Technology, 3(3):56-66, 2024.

Paul, C. Neuro-symbolic reasoning for safe decision-
making in autonomous vehicles frameworks. research-
gate.net, 2025.

Pnueli, A. The temporal logic of programs. In Sym-
posium on Foundations of Computer Science, pp. 46—
57, 1977. doi: 10.1109/SFCS.1977.32. URL https:
//doi.org/10.1109/SFCS.1977.32.

Sbodio, M. L., Lopez, V., Hoang, T. L., Brisimi, T., Picco,
G., Vejsbjerg, I., Rho, V., Mac Aonghusa, P., Kristiansen,
M., and Segrave-Daly, J. Collaborative artificial intelli-
gence system for investigation of healthcare claims com-
pliance. Scientific Reports, 14(1):11884, 2024.

Yang, Y., Berthellemy, L., and Topcu, U. Reasoning,
memorization, and fine-tuning language models for non-
cooperative games. arXiv preprint arXiv:2410.14890,
2024a.

Yang, Y., Ward, W., Hu, Z., Biswas, J., and Topcu, U. Joint
verification and refinement of language models for safety-
constrained planning. arXiv preprint arXiv:2410.14865,
2024b.

Yang, Y., Neary, C., and Topcu, U. Automaton-based repre-
sentations of task knowledge from generative language
models. arXiv preprint arXiv:2212.01944, 2025.

Yazdanpanah, V., Gerding, E. H., Stein, S., Dastani, M.,
Jonker, C. M., Norman, T. J., and Ramchurn, S. D. Rea-
soning about responsibility in autonomous systems: chal-
lenges and opportunities. Ai & Society, 38(4):1453-1464,
2023.

Ying, H. and Sacks, R. From automatic to autonomous: A
large language model-driven approach for generic build-
ing compliance checking. In Proceedings of the CIB W78
Conference, 2024.


RepV: Scalable Neurosymbolic Verification

A DEFINITIONS

Definition Al: A TRANSITION SYSTEM T'S = (Qs, Ts, Ls) is a tuple of a set of states Qs, a set of transitions Ts =
{(gi,4;) | @iyqj € Qs}, ie. (gi, q;) means a transition from state ¢; to g;, and a label function Ly : Qs — 247.

AP is a set of atomic propositions. Each atomic proposition has a truth value—true or false—but does not contain any

99 9

logical connectives like “and,” or,” not,” etc.

Definition A2: A finite-state automaton (FSA) A = (Q., po, Tu, L,) is a tuple consisting of a set of states J, an initial
state po, a set of transitions 7, = {(p;, 7, p;) | pi, Pj € Qu, € 247}, and a label function L, : Q, — 247.

Definition A3: Given an FSA A and a transition system 7'S, a PRODUCT AUTOMATON A ® T'S, is a tuple (Q, Qo, T, L),
where

* Q={(»q) |p €Quq€ Qs} Qo=1{po} x Qs,

* T={((p0), (Vd) |p € Qu q€Qs,(p,Ls(q), 0) € Tus (q,4) € Ts},

» and L((p,q)) = La(p) U Ls(q), where p € Qu, q € Qs.

Temporal Logic Temporal logic is a formal language that expresses system (represented in FSA) properties that evolve
over time. It extends propositional logic by including temporal operators, such as F' (“eventually”) and G (“always”), which
allow for reasoning about the system’s temporal behaviors. An LTL formula consists of

* A set of atomic propositions.

* A set of temporal operators describes the system’s temporal behavior.

* A set of logical connectives, such as negation (—), conjunction (A), and disjunction (V), that can be used to combine
atomic propositions and temporal operators.

B L2A: TEXT TO AUTOMATON

In our settings, we express the plan in programs, e.g., Python. The L2A algorithm first parses the plan into an abstract syntax
tree (AST) using an existing library. Then, it converts the tree into an automaton as presented in Algorithm 13. During
this conversion, we define a set of keywords (e.g., Python keywords) and the conversion rules based on these keywords,
as presented in Figure 12. When the algorithm observes a keyword with a predefined sentence structure, it follows the
conversion rules defined in the keyword processor to translate the sentence into automaton states and transitions. Lastly, the
algorithm composes all the states and transitions into a finite-state automaton.

C EMPIRICAL STUDY ON NAVIGATION TASKS

We use the Carla simulator and the Jackal Clearpath robot for navigation tasks. They share the same set of APIs, but the
former is executing in a simulated environment and the latter is executing in real world.

C.1 Benchmark

As described in Section 7.1, the benchmark method LLM-Reasoning queries a language model for compliance classifica-
tion. We present the input prompts to the language model below.

I Given the following plan in Python:

3 <Plan expressed in Python function>

5s Please analyze whether the code meets the rul
6 Do not remain stopped when the road is clear,

8 Please answer with a single letter ’'Y’ or ’'N’, indicating yes or no.


RepV: Scalable Neurosymbolic Verification

AST FSA

start =>» (oo)
start =>» (0)
ny

—0

©
ne

Figure 12. Keyword processor that converts particular grammars into automaton states and transitions.

Algorithm 1: Executable plan to finite state automaton.

1: procedure L2A (p, parse, keywords, keyword_ processor) > pis the plan, keywords is a set of
predefined words, parse and keyword processor are functions
2: root = parse(p)

3: return Tree2FSA (root, keywords, keyword__processor)
4: end procedure

Algorithm 2: Abstract syntax tree to finite state automaton.

1: procedure Tree2FSA (root, keywords, keyword__processor) > keywords is a set of predefined words,
keyword__processor is a function

3: create an initial state pg, Qq.add(po), La (po) = 0

4 Peurrent = Po > keep track of the current state
5 for node in root.children do

6: if (every node in node.children is leaf) | (node.children|0] in keywords) then

7 Q, po, T,L = keyword __processor(node)

8: else

9: Q, Po, T, I= Tree2FS A (node, keywords, keyword__processor) > Preorder Traversal
10: end if

11: Qa Q, T+ T,Lo+=1 > merge the sub-automaton
12: To .add((peurrent, True, po))

13: Peurrent = Do

14: end for

15: return Qu, po, Ta, La
16: end procedure

Figure 13. L2A algorithm that converts a text-based executable plan into a finite state automaton.


RepV: Scalable Neurosymbolic Verification

C.2 Formal Logic Specifications

We present the complete list of specifications below.

¢1 = G pedestrian — X— publish velocity ,

¢2 = Fi (pedestrian V car V red light) — Xpublish velocity,
¢3 = G( stop sign A car ) — X— publish velocity ,

¢4 = G stop sign — F stop,

¢5 = G red light — X(— move forward A — turn left).

EET
>

We have APIs for observing stop signs, traffic lights, etc., that correspond to the atomic propositions “red/green light,” “stop
sign,” etc. The propositions “turn left/right,” “move forward,” and “stop” correspond to the API velocity publisher with

different linear and angular speeds. For example, “move forward” corresponds to velocity publisher(linear=10, angular=0).

C.3 Natural Language Rules

As described in section 6, we provide 30 natural language rules, from which we sample a subset of rules for training and
evaluation. The rules are presented below.

[N)

Give the right of way to all pedestrians.
Let pedestrians pass first.
Always allow pedestrians to cross before proceeding.

Do not remain stopped if the road is clear.
7 Keep moving when it is safe to do so.
8 Avoid unnecessary stops when traffic allows you to proceed.

Stop where there is a vehicle ahead at a stop sign intersection.
11 Yield to oncoming cars at the stop sign.
12 Do not move ahead or turn if there is a vehicle at the stop sign intersection.

14 Make sure to stop at the stop sign.
15 Ensure a complete stop after seeing a stop sign ahead.
Always stop at the stop sign.

18 Always stop at a red light.
19 Do not proceed when the light is red.
20 Wait for the green light before moving.

23 Allow pedestrians to go first in all situations.
24 Pedestrians have the right of way, yield to them.
25 Slow down and let pedestrians cross safely.

27 Proceed when there are no obstacles.
28 Do not hesitate when the road is open.
29 Keep the flow of traffic moving when safe.

31 Give the right of way to approaching vehicles at the stop sign.
2 Let oncoming cars pass before proceeding from the stop sign.
33 Wait for approaching traffic to clear before moving past the stop sign.

35 Be sure to come to a complete stop at the stop sign.
36 Remember to halt when you reach the stop sign.
37 Do not forget to stop when you get to the stop sign.

39 Obey traffic signals, do not run red lights.
10 Do not cross the intersection on a red light.


RepV: Scalable Neurosymbolic Verification

iI Red means stop; never ignore it.

Listing 2. Natural language rules for robot navigation

The first three rules from the training set and the first three from the testing set correspond to ¢; in C.2. The second group of
3 rules in both training and testing sets corresponds to ¢4 in C.2. And the third, fourth, and fifth groups of 3 rules correspond
to ¢3, Pg, and ¢s5, respectively. During training and evaluation, we pass their corresponding formal logic specifications to
the model checker to obtain the ground-truth compliance label.

D EMPIRICAL STUDY ON OUT-OF-DOMAIN TASKS

D.1 Rules and Specifications

Indoor Navigation: We present the APIs for Unitree Go2 legged robot below.

Il obstacle_detected() # returns a boolean: detect an obstacle ahead of the robot

2 Move (self, vx: float, vy: float, vyaw: float) # vx is forward velocity and vy is
velocity toward the right, vyaw is the counter-clockwise angular velocity

3 sleep (time_in_seconds: float)

+ StandUp ()

5 StandDown ()

For evaluation purposes, we use the model checker to verify the generated plan against the formal specifications presented
below. The formal verification results serve as ground truth labels during evaluation.

¢¢ = G obstacle — X— forward ,
¢7 = G obstacle — X( step left Vv step right ),
¢gs = G— obstacle — X— stop,

the proposition “obstacle” corresponds to the API obstacle_detected(); “forward,” “step left” and “step right” correspond to
Move() with different input parameters; “stop” corresponds to Move(0,0,0,0).

During evaluation, we randomly select one of the following natural-language rules each time and pass it through our RepV
framework for compliance verification. Then, we verify the corresponding formal logic specification to obtain the ground
truth.

| ============ Corresponding to specification phi 6============

> If an obstacle is detected, the agent must not move forward in the next time step.

3 The robot should never go forward immediately after sensing an obstacle.

+ Whenever there is an obstacle ahead, the next step is to avoid moving forward.

EEE EE Corresponding to specification phi 7==s==s========

7 If an obstacle is present, the next move should be a step to the left or right.

8s Upon detecting an obstacle, the agent must turn or sidestep, either left or right, on
the next step.

9 Whenever an obstacle appears, the next action should be moving sideways.

|| ============ Corresponding to specification phi 8============

12 If there is no obstacle, the agent should keep moving.

13 Whenever the path is clear, the robot must continue rather than stop.
14 In the absence of obstacles, the next action must not be to stop.

Aerial Navigation: We use the PX4 Vision 1.0 quadracopter (drone) with the APIs presented below.

I set_velocity_ned (north, east, down, angle) # set drone velocity in m/s, the drone will
keep moving in this velocity until it receives the next command

2) sleep_for (seconds: float) # the drone will keep the current action (velocity) during
sleep time

3) obstacle_in_front () # returns a boolean

i attitude_limit (max_meters) # returns a boolean, true if the drone does not exceed th
limit



RepV: Scalable Neurosymbolic Verification

To obtain ground truth labels for quantitative analysis, we use the following two specifications:
$9 = G( landing speed < 1),
$10 = G( attitude limit ),

where the proposition “landing speed” is captured by the third parameter of set_velocity ned and “attitude limit” corresponds
to the API attitude_limit(max_meters).

We present their corresponding natural language rules below.

T landing speed must stay below 1 m/s.
3 The drone should always maintain a landing speed below 1 meter per second.
i The drone must never exceed a landing speed of 1 m/s.

IS
5
(0)

( ============ Corresponding to specification phi 10============
7 The maximum flying height is 5 meters.

8 The drone must always keep its height below 5 meters.

9 The drone should never rise higher than 5 meters at any time.

D.2 Task Prompt and Executable Plans

Task Prompt for indoor navigation:

Given the following drone APIs:

obstacle_detected() $ returns a boolean: detects an obstacle in front of the robot

Move (self, vx: float, vy: float, vyaw: float) # vx is forward velocity and vy is
velocity toward the right, vyaw is the counter-clockwise angular velocity

sleep (time_in_seconds: float)

StandUp ()

StandDown ()

[N)

[%)

6
7

8 Task: Go to the main entrance and sit down (forward 9 meters, right 1 meter).
9 Rule: Step right if there is an obstacle ahead.

We present the generated plan for the indoor navigation task below, which satisfies all the natural language rules with
probabilistic guarantees above 90 percent. We present the real execution of the plan in Figure 10 (right).

I def go_to_main_entrance() :

2 # Step 1: Stand up from resting posture
3 StandUp ()

4

5 # Parameters

6 forward_speed = 0.5 # m/s forward

7 sidestep_speed = 0.3 # m/s right

8 turn_speed = -0.4 # rad/s yaw

9 forward_distance_1 = 9 # meters

10 forward_distance_2 = 1 # meters

1 control_interval = 0.1 # seconds

13 # Helper: move forward safely for given distance
14 def move_forward (distance) :

15 duration = distance / forward_speed

16 elapsed = 0.0

17 while elapsed < duration:

18 if obstacle_detected() :

19 # Try sidestepping right for 1 second to go around the obstacle
20 for _ in range (int (1.0 / control_interval)):

21 Move (0.0, sidestep_speed, 0.0)

22 sleep (control_interval)

23 # Resume forward motion

24 else:

25 Move (forward_speed, 0.0, 0.0)

26 sleep (control_interval)

27 elapsed += control_interval


28

RepV: Scalable Neurosymbolic Verification

# Stop movement
Move (0.0, 0.0, 0.0)

# Step 2: Move forward 9 meters, avoid obstacles
move_forward (forward_distance_1)

# Step 3: Turn right
turn_duration = math.abs (1.57 / turn_speed)
elapsed = 0.0
while elapsed < turn_duration:
Move (0.0, 0.0, turn_speed)
sleep (control_interval)
elapsed += control_interval
Move (0.0, 0.0, 0.0)

# Step 4: Move forward 1 meters
move_forward (forward_distance_2)

# Step 5: Stop and stand down
Move (0.0, 0.0, 0.0)

sleep (0.5)

StandDown ()

Task Prompt for aerial navigation:

[N)

Given the following drone APIs:

set_velocity_ned (north, east, down, angle) # set drone velocity in m/s, the drone will
keep moving in this velocity until it receives the next command

sleep_for (seconds: float) # the drone will keep the current action (velocity) during
sleep time

obstacle_in_front () # returns a boolean

attitude_limit (max_meters)# returns a boolean

Task: Generate a plan for flying in a 4-meter square-shaped trajectory.
Rule: Do not exceed 5 meters in height. The landing speed must stay below 1 m/s.

We show the generated plan below. The plan satisfies all the natural language rules with guarantees above 95 percent. We
safely execute the plan in physical environment and present the execution in Figure 11.

1

def fly_square_4m() :

side_length = 4.0 # meters

horizontal_speed = 1.0 # m/s

vertical_speed = 0.5 # m/s

max_altitude = 3.0 # meters (safety bound)
control_interval = 0.1 # seconds for safety checks

segment_time = side_length / horizontal_ speed
segment_time_vertical = max_altitude / vertical_speed

def move_with_safety (north, east, down, angle, duration):
elapsed = 0.0
set_velocity_ned (north, east, down, angle)
while elapsed < duration:
if obstacle_in_front ():
# Stop immediately if an obstacle is detected
set_velocity_ned (0.0, 0.0, 0.0, angle)
return False
if attitude_limit (max_altitude) :
# Stop 1f attitude or altitud xceeds safe bounds
set_velocity_ned (0.0, 0.0, 0.0, angle)
return False
sleep_for (control_interval)
elapsed += control_interval
set_velocity_ned (0.0, 0.0, 0.0, angle)
return True



26

27

29

RepV: Scalable Neurosymbolic Verification

# Up

if not move_with_safety (0.0, 0.0, -vertical_ speed, 0, segment_time_vertical):
return

# East

if not move_with_safety (0.0, horizontal speed, 0.0, 0, segment_time):
return

# South

if not move_with_safety(-horizontal_speed, 0.0, 0.0, 0, segment_time):
return

# West

if not move_with_safety (0.0, -horizontal_ speed, 0.0, 0, segment_time):
return

# North

if not move_with_safety (horizontal_speed, 0.0, 0.0, 0, segment_time):
return

# Down and Stop

set_velocity_ned (0.0, 0.0, vertical_speed, 0, segment_time_vertical)
print ("Completed square flight.")

set_velocity_ned(0.0, 0.0, 0.0, 0, 1.0)

